pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                input{
                    string(name:'String', defaultValue:'None', description:'input string')
                }
            }
        }
    }
}
